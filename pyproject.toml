[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "hyprgrok"
version = "0.3.0"
description = "Hyprland companion panel for official Grok Build — no xAI API keys"
readme = "README.md"
license = { text = "MIT" }
requires-python = ">=3.12"
authors = [{ name = "HyprGrok contributors" }]
keywords = ["hyprland", "grok", "wayland", "ai", "companion", "xai"]
classifiers = [
  "Development Status :: 4 - Beta",
  "Environment :: X11 Applications",
  "Intended Audience :: Developers",
  "License :: OSI Approved :: MIT License",
  "Operating System :: POSIX :: Linux",
  "Programming Language :: Python :: 3",
  "Programming Language :: Python :: 3.12",
  "Programming Language :: Python :: 3.13",
  "Topic :: Desktop Environment",
]
dependencies = []

[project.scripts]
hyprgrok = "hyprgrok.main:main"

[project.urls]
Homepage = "https://github.com/dcbert/hyprgrok"
Issues = "https://github.com/dcbert/hyprgrok/issues"

[tool.setuptools.packages.find]
include = ["hyprgrok*"]

[tool.setuptools.package-data]
hyprgrok = []
